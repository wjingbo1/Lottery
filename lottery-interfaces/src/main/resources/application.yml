server:
  port: 80

spring:
  datasource:
    username: root
    password: "1767737316."
    url: jdbc:mysql://localhost:3306/bugstack?useUnicode=true
    driver-class-name: com.mysql.jdbc.Driver

mybatis:
  mapper-locations: classpath:/mybatis/mapper/*.xml
  config-location:  classpath:/mybatis/config/mybatis-config.xml
